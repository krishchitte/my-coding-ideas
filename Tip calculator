print("Welcome to the tip calculator!")
bill = input("What was the total bill?\n")
tip = input("what percentage of tip would you like to give? 10, 12, or 15? \n")
peoples = input("how many people to split the bill?\n")

real_tip = float(bill) * (int(tip)/100) 
real_bill = float(bill) + float(real_tip)
divided_bill = round(float(real_bill) / int(peoples), 2)

print(f"each person has to pay {divided_bill}")
